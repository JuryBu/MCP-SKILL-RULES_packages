## Windsurf 渲染与交付

先确认当前 IDE 的 Markdown、公式和本地文件链接渲染能力。若不支持 LaTeX，就用该宿主支持的 HTML、图片或 PDF 交付公式，并先截图检查渲染结果。交付文件时给出接收方环境中稳定可访问的链接或路径，避免临时会话路径。

表格中避免会被渲染器误识别为链接或标记的裸字符；复杂内容改用简短段落或代码格式。图片逐张展示以保留清晰度。PDF、Office 和图片交付前做截图或预览检查，确认没有空白、溢出或重叠。
