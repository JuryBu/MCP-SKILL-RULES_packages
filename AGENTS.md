# 共享维护仓库协作

本仓库由多个维护任务共同使用，每台机器只使用各自已确认的唯一主工作目录和 `main` 分支。不要为调查、实现、审查、发布或解决冲突新建分支、worktree、clone、整仓备份或另一套可继续开发的源码副本；构建和测试的临时产物不作为第二个开发或发布源。

## 修改与提交

- 开工先核对所在主目录、分支、远端、提交基线、暂存区和未提交改动；发现落后或交叉修改，先查提交来源与维护任务，不把旧内容当成有意回退。
- A、B 任务同时工作时可修改互不重叠的范围；同一文件或依赖链有交叉时先沟通分工与先后顺序，由先完成并验收的一方精确提交，另一方核对结果后接续修改。
- 共享暂存区和 Git 写操作必须串行协调，不同时暂存、提交、合并或推送，不使用 `git add .` 混入其它任务的改动；不能用新副本回避协调。
- 提交前核对完整差异、来源与针对性验收，推送前检查远端是否变化；有冲突回到对应维护任务确认，不覆盖、强推或擅自丢弃对方成果。

## 历史整理与运行安全

已有分支、副本与备份先登记所有权、独有改动、提交和运行依赖，与负责的维护任务确认后逐项归并、清理；禁止为满足“只保留一个”而直接批量删除、重置或强制覆盖。

生产安装目录不是第二个源码维护目录。源码归并不自动授权升级、切换或重启生产，也不取消必要的配置、持久数据及已部署运行版本回退措施；不得把运行回退备份当成新的开发或发布来源。
