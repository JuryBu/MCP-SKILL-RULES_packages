/**
 * 程序版本号单一来源。
 *
 * 此前 "1.15.14" 散落在 index.ts 的 McpServer 配置、启动日志、使用指南标题等多处硬编码，
 * 升版时易漏改导致各处不一致。统一收敛到这里，所有「程序版本声明」一律引用 VERSION。
 *
 * 注意：changelog / 使用指南正文里描述「v1.x.y 起如何如何」的历史特性说明属于文档内容，
 * 不是版本声明，保持原字面量、不引用此常量（升版时这些历史记述不应跟着变）。
 */
export const VERSION = "1.17.1";
